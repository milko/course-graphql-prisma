#endpoint: ${env:PRISMA_ENDPOINT}
endpoint: https://eu1.prisma.sh/milko-skofic/course-graphql-prisma/dev
datamodel: datamodel.graphql
secret: ${env:PRISMA_SECRET}
# Remember to run 'prisma token' in prisma directory
# to get the Bearer token for the :4466 playground.